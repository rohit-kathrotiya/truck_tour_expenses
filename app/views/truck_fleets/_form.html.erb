<%= form_with(model: truck_fleet) do |form| %>
  <% if truck_fleet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(truck_fleet.errors.count, "error") %> prohibited this truck_fleet from being saved:</h2>

      <ul>
        <% truck_fleet.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
